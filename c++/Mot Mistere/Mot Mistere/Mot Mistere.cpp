#include <iostream>

using namespace std;

string words[10] = { "Plane", "Brother", "Sister", "Mother", "Father", "Game", "School", "Season", "Battle", "Friendship" };

class Word {
	string word;
	string shuffeledWord;

public:
	Word() {
		word = words[0];
	}
};

int main() {
	
	return 0;
}